#include <stdio.h>
int main(void) {
    int i = 10;
    while(i=7) printf("ronaldo is the goat\n");    // notice how its = and not == which implies an infinite loop runs as 10 can never be equal to 7
    return 0;
}